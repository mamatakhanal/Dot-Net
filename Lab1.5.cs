// Write a C# program to print the result of the specified operations-
// Take two input number and one operator from user

using System;
namespace Lab1
{
    internal class Operator
    {
       /* static void Main(string[] args)
        {
            Console.WriteLine("Enter first number : ");
            string num1 = Console.ReadLine();
            int Num1 = Convert.ToInt32(num1);

            Console.WriteLine("Enter second number : ");
            string num2 = Console.ReadLine();
            int Num2 = Convert.ToInt32(num2);

            Console.WriteLine("Enter Operator : ");
            string o = Console.ReadLine();
            int result;

            if (o == "+")
            {
                result = Num1 + Num2;
            }
            else if (o == "-")
            {
                result = Num1 - Num2;
            }
            else if (o == "*")
            {
                result = Num1 * Num2;
            }
            else
            {
                result = Num1 / Num2;
            }
            Console.WriteLine(Num1 + o + Num2 + "=" + result);
            Console.ReadLine();
        } */
    }
}
