// Write a program in C# to convert a string array to a string.

/* using System;
namespace Lab1
{
    internal class String
    { 
       static void Main(string[] args)
        {
            // Define a string array
            string[] stringArray = { "Apple", "Banana", "Cat", "Dog", "Egg", "Fish" };

            // Convert the string array to a single string
            string result = string.Join(" ", stringArray);

            // Output the result
            Console.WriteLine("\nString array convert to a string:");       
            Console.WriteLine(result);
            Console.ReadKey();
        }
    }
}   */
